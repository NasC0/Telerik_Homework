using System;
using GSMType.Common;

class Encapsulation
{
    static void Main()
    {
        GSM gsm = new GSM("Teehee", "Manufacturer", 5.5m);
        // Try accessing the encapsulated fields
    }
}
