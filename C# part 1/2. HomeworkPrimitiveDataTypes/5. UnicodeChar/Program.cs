using System;
class unicodeChar
{
    static void Main()
    {
        char unicodeChar = '\u0048'; //initialising the unicode character, 72 in decimal format
        //test, should print out H
        Console.WriteLine(unicodeChar);
    }
}
